@model IEnumerable<WeCare_PU.Entities.Messagerie>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Message)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Message)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MessagerieID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MessagerieID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MessagerieID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
