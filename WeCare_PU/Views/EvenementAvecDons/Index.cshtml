@model IEnumerable<WeCare_PU.Entities.EvenementAvecDon>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Montant)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Titre)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Description)
                </th>
            <th>
                @Html.DisplayNameFor(model => model.Association)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Montant)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Titre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Association.Discriminator)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EvenementID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EvenementID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EvenementID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
